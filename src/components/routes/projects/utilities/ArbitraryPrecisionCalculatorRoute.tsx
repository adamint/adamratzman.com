import { Text } from '@chakra-ui/react';

export function ArbitraryPrecisionCalculatorRoute() {
  return <>
    <Text>The arbitrary precision calculator will be re-added in the near future. Sorry :(</Text>
  </>;
}